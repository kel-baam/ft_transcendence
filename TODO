=>password in intra and google
=>online friends
=>blocked friends (done)
=>serach how can i user API gatway websockets (done)
=>crf token 
=>icons in profile
=>password forget passowrd problem(kaoutar mission's)
=> email (the errors in format not detected)
=> handle error form request to /api/user(kaoutar mission's)
=> remove icon camera from the profile of the other user 

=> search bar in blocked friends (done)

=> to add later (set status of the user to true)

=> problem in settings
// location /ws/ {
//         proxy_pass http://backend:8000/;  # WebSocket backend
//         proxy_http_version 1.1;
//         proxy_set_header Upgrade $http_upgrade;
//         proxy_set_header Connection "Upgrade";
//         proxy_set_header Host $host;
//         proxy_set_header X-Real-IP $remote_addr;
//         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
//     }

merge 
=> models 

=>logo clicked navigate to 

password err while registring(done)

undifiend when chnaged password(done)


Major module: Use a Framework as backend 

Minor module: Use a database for the backend

Major module: Implementing a remote authentication

Major module: Remote players

Major module: Implement Two-Factor Authentication (2FA) and JWT.

Major module: Designing the Backend as Microservices

Major module: Standard user management, authentication, users across
tournaments.

put in plac of the leaderboard and top 5 the content when no user play a match 

=>i add registration_type in data got from intra and google(done)

=>handle error come from /api/user

=>here was an error ''if hasattr(user, 'enabled_twoFactor') and user.enabled_twoFactor:'' i change it with this one 

=>duplicate email in intra should display error (kaoutar's mission)


new tasks for last push ===>
{
    gmail field doesn't check errors for valid gmail
    password doesnt check id id strong or not


    check incons and 404 eroors 
    dlete volume add .ignore fro .env


    check ports if everyone didi 3000

    check settings update 
    delete badges n leaderboard
    winner announce in loal game

    time for match history
    lokced badges
    check settings icon

    duplicate username and email
    


}